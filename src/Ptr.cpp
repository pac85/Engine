#include "Ptr.h"
#include "Base.h"

/*template<class T>
Ptr<T>::operator =(const string s)
{
    if(obj)obj->Release();
    obj= BaseFactory::createInstance(s);
    if(obj)obj->AddRef();
}*/
